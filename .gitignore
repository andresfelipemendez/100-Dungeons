*.DS_Store
.vs

# build dirs: the engine's own (meikyu/build*) and every project's --
# all artifacts are engine-generated, never tracked
build/
build-linux/
build-mac/

# compiled test binaries (kaji/seni test runners emit these)
*.out
