# meikyu project marker -- the engine generates every build input from
# this folder's src/ tree. See meikyu/src/platform/project_gen.h.
name dungeon1

# nested hot-state structs whose definitions seni must see: prepend each lib's
# state header to the game_state.h snapshot so the reload diff resolves the field.
layout_include lib/henshu/henshu_state.h
layout_include lib/mono/mono_state.h
