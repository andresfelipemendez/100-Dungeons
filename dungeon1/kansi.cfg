# kansi (監視) watch config for dungeon1 -- watch-only: kansi reports a
# debounced change edge and the host hands the rebuild to kaji (kaji.cfg
# target 'game'). One file for every platform; building knowledge lives
# entirely in kaji.cfg.

watch src
watch ../meikyu/src
ext .c .h .vert .frag
debounce_ms 30
